# numpygrad
A lightweight, reverse-mode automatic differentiation engine and neural network framework built entirely from scratch in pure NumPy. Features dynamic computational DAGs, broadcasting-aware backpropagation, AdamW, and modular layers—zero PyTorch/TensorFlow dependencies.
